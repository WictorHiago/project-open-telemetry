# instalacao express
npm install express
npm install @types/express

# instalacao cors
npm install cors
npm install @types/cors

# instalcal morgan
npm install morgan
npm install @types/morgan

# instalacal helmet
npm install helmet
npm install @types/helmet

# instalacao Drizzle - PG - Dotenv - Types PG
npm i drizzle-orm pg dotenv
npm i -D drizzle-kit tsx @types/pg

# instalacao tsyringe
npm install tsyringe reflect-metadata

# servicos docker
cd docker
docker compose up -d

# pgadmin banco de dados
disponivel em: http://localhost:5050
usuario: admin@admin.com
senha: admin

# postgres banco de dados
name: localhost
hostname/address: postgres
port: 5432
usuario: telemetry
senha: telemetry

database: TELEMETRY
schema: public

# teste com jest
npm install --save-dev jest

npm install --save-dev ts-jest @types/jest supertest @types/supertest